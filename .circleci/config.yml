version: 2
jobs:
  build:
    docker:
      - image: circleci/node:9.6
    environment:
      TZ: /usr/share/zoneinfo/UTC
    steps:
      - checkout
      - run: git submodule sync
      - run: git submodule update --init
      - restore_cache:
          key: dependency-cache-{{ checksum "package.json" }}
      - run: sudo apt update && sudo apt install python-pip python-dev
      - run: pip install awscli --upgrade --user
      - run: echo 'export PATH=/home/circleci/.local/bin:$PATH' >> $BASH_ENV
      - run: yarn --version
      - run: yarn install
      - run: npm run build
      - save-cache:
          key: dependency-cache-{{ checksum "package.json" }}
          paths:
            - ~/.local
            - ~/.npm/_cacache
            - ~/.cache/yarn
            - node_modules/
      - save-cache:
          key: artifact-cache-{{ .Environment.CIRCLE_SHA1 }}
          paths:
            - lib/
            - lib-es5/
            - ref/
            - register/
            - test-es5/
            - yarn.lock
            - src/index.ts
  test:
    docker:
      - image: circleci/node:9.6
    environment:
      TZ: /usr/share/zoneinfo/UTC
    steps:
      - checkout
      - run: git submodule sync
      - run: git submodule update --init
      - restore_cache:
          key: dependency-cache-{{ checksum "package.json" }}
      - restore_cache:
          key: artifact-cache-{{ .Environment.CIRCLE_SHA1 }}
      - run: sudo apt update && sudo apt install python-pip python-dev
      - run: pip install awscli --upgrade --user
      - run: echo 'export PATH=/home/circleci/.local/bin:$PATH' >> $BASH_ENV
      - run: npm test
      - run: sh ./common/e2e.sh
      - run: sh release.sh
      - run:
          name: Move Artifacts
          command: mv register /tmp/artifacts/
      - store_artifacts:
          path: /tmp/artifacts/register
  deploy_master:
    docker:
      - image: circleci/node:9.6
    environment:
      TZ: /usr/share/zoneinfo/UTC
    steps:
      - checkout
      - run: git submodule sync
      - run: git submodule update --init
      - restore_cache:
          key: dependency-cache-{{ checksum "package.json" }}
      - restore_cache:
          key: artifact-cache-{{ .Environment.CIRCLE_SHA1 }}
      - run: sudo apt update && sudo apt install python-pip python-dev
      - run: pip install awscli --upgrade --user
      - run: echo 'export PATH=/home/circleci/.local/bin:$PATH' >> $BASH_ENV
      - run: npm run doc
      - run:
          name: Move Artifacts
          command: mv docs /tmp/artifacts/
      - store_artifacts:
          path: /tmp/artifacts/docs
      - run:
          name: document deploy
          command: aws s3 cp /tmp/artifacts/docs/ s3://api.grimoire.gl/$CIRCLE_PROJECT_REPONAME --recursive --acl public-read

workflows:
  version: 2
  workflow:
    jobs:
      - build
      - test:
          requires:
            - build
      - deploy_master:
          requires:
            - build
            - test
          filters:
            branches:
              only: master
